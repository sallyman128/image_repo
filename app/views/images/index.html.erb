<p id="notice"><%= notice %></p>

<h1>Images Index</h1>

<table>
  <tbody>
    <% @images.each do |image| %>
      <tr>
        <div class="image" style="width:400px; height:auto">
          <td><%= link_to image_tag(image.picture, style:"width:20%"), image_path(image) %></td>
        <div>
      </tr>
    <% end %>
  </tbody>
</table>